/*
 * stubs.c — at the end of G3 every module that editor.c calls has been
 * grafted. This file now exists only to host stubs we still need for
 * features genuinely not yet wired in our shell.
 *
 * Currently empty; kept in the CMake source list so adding future stubs
 * doesn't require touching CMakeLists.txt.
 */
